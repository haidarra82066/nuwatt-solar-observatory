{
  "openapi": "3.1.0",
  "info": {
    "title": "NuWatt Open Solar Observatory API",
    "version": "1.2.0",
    "description": "Read-only public contracts for the real Lebanon large-solar AI screening release, the LCEC research benchmark, and the separate synthetic foundation contract."
  },
  "servers": [{ "url": "/api/v1" }],
  "paths": {
    "/benchmark": {
      "get": {
        "summary": "Get the published national capacity benchmark",
        "description": "Returns market-capacity context that is not an AI count or location dataset.",
        "responses": { "200": { "description": "LCEC national and governorate capacity context" } }
      }
    },
    "/health": {
      "get": {
        "summary": "Check API and release status",
        "responses": { "200": { "description": "Service status" } }
      }
    },
    "/screening": {
      "get": {
        "summary": "Get the current public AI screening release",
        "description": "Returns 5 km privacy-safe cells for the January 2024 Satlas large-solar screening experiment. Evidence states are screened or corroborated; no capacity or technical yield is emitted.",
        "responses": { "200": { "description": "Versioned screening GeoJSON FeatureCollection with provenance metadata" } }
      }
    },
    "/summary": {
      "get": {
        "summary": "Get release-wide summary metrics",
        "responses": { "200": { "description": "Summary with P10/P50/P90 ranges" } }
      }
    },
    "/cells": {
      "get": {
        "summary": "Get generalized evidence cells as GeoJSON",
        "parameters": [
          {
            "name": "status",
            "in": "query",
            "schema": { "type": "string", "enum": ["detected", "estimated", "verified"] }
          },
          {
            "name": "governorate",
            "in": "query",
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": { "description": "GeoJSON FeatureCollection" },
          "400": { "description": "Invalid filter" }
        }
      }
    },
    "/municipalities": {
      "get": {
        "summary": "Get municipality aggregates",
        "responses": { "200": { "description": "Municipality ranking" } }
      }
    }
  }
}
